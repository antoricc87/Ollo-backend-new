node_modules/
dist/
*.log
.DS_Store

# secrets — never commit environment files or backups of them
.env
.env.*
*.bak

# service-account key slot (placeholder today, real key someday)
src/config/firebaseConfig.json

# user-uploaded content
uploads/

# nutrition eval reports
eval-results/

# real lab reports used for local extraction evals (PHI — never commit)
docs/lab-samples/
